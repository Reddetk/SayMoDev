# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: '3'

vars:
  ENV: '{{default "local" .ENV}}'

dotenv: ['.env.{{.ENV}}']

tasks:

  # ---------------------------------------------------------------------------
  # RSA keys
  # ---------------------------------------------------------------------------

  gen-rsa:
    desc: "Generate RSA-2048 key pair in testdata/keys/"
    cmds:
      - mkdir -p testdata/keys
      - openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048 -out testdata/keys/private.pem
      - openssl rsa -pubout -in testdata/keys/private.pem -out testdata/keys/public.pem
    generates:
      - testdata/keys/private.pem
      - testdata/keys/public.pem

  # ---------------------------------------------------------------------------
  # Infrastructure
  # ---------------------------------------------------------------------------

  infra:up:
    desc: "Start postgres + redis (detached). Postgres healthcheck is managed by docker compose."
    cmds:
      - docker compose up -d postgres redis-bc1 redis-blacklist migrate-init 
      - docker compose ps

  infra:down:
    desc: "Stop infra containers (volumes preserved)"
    cmds:
      - docker compose down

  infra:destroy:
    desc: "Stop containers AND delete volumes (full reset)"
    cmds:
      - docker compose down -v

  infra:logs:
    desc: "Tail infra container logs"
    cmds:
      - docker compose logs -f postgres redis-bc1 redis-blacklist

  # ---------------------------------------------------------------------------
  # Migrations
  # Запуск напрямую через go run -- для быстрой итерации без сборки образа.
  # Гарантия готовности postgres -- на стороне вызывающего кода (infra:up до migrate:up).
  # ---------------------------------------------------------------------------

  migrate:up:
    desc: "Apply all pending migrations"
    cmds:
      - go run ./cmd/migrate/main.go -action up

  migrate:down:
    desc: "Roll back the last migration"
    cmds:
      - go run ./cmd/migrate/main.go -action down

  migrate:force:
    desc: "Force-set schema version. Use: task migrate:force VERSION=N"
    cmds:
      - go run ./cmd/migrate/main.go -action force -version {{.VERSION}}
    requires:
      vars: [VERSION]

  # ---------------------------------------------------------------------------
  # Server
  # ---------------------------------------------------------------------------

  run:
    desc: "Run identity-service locally"
    cmds:
      - go run ./cmd/main.go

  build:
    desc: "Build server binary -> bin/server"
    cmds:
      - mkdir -p bin
      - go build -ldflags="-s -w" -o bin/server ./cmd/main.go

  build:migrate:
    desc: "Build migrate binary -> bin/migrate"
    cmds:
      - mkdir -p bin
      - go build -ldflags="-s -w" -o bin/migrate ./cmd/migrate/main.go

  # ---------------------------------------------------------------------------
  # Docker full stack
  # ---------------------------------------------------------------------------

  docker:up:
    desc: "Build and start full stack via docker compose (includes migrate-init)"
    cmds:
      - docker compose up --build -d
      - docker compose ps

  docker:down:
    desc: "Stop full stack"
    cmds:
      - docker compose down

  docker:logs:
    desc: "Tail server logs"
    cmds:
      - docker compose logs -f server

  # ---------------------------------------------------------------------------
  # Dev (local go run + docker infra)
  # Гарантия порядка: infra:up должен завершиться и postgres должен стать healthy
  # до запуска migrate:up. Если postgres ещё не готов -- подожди 30s и повтори task migrate:up.
  # ---------------------------------------------------------------------------

  dev:
    desc: "Start infra then run migrate + server locally"
    cmds:
      - task: infra:up
      - task: migrate:up
      - task: run

  # ---------------------------------------------------------------------------
  # Dev helpers
  # ---------------------------------------------------------------------------

  tidy:
    desc: "go mod tidy"
    cmds:
      - go mod tidy

  lint:
    desc: "Run golangci-lint"
    cmds:
      - golangci-lint run ./...

  test:
    desc: "Run unit tests"
    cmds:
      - go test ./... -count=1 -race

  test:cover:
    desc: "Run tests with HTML coverage report"
    cmds:
      - go test ./... -count=1 -coverprofile=coverage.out
      - go tool cover -html=coverage.out -o coverage.html
